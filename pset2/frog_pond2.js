const activities = ["babysit tadpoles", "flies for lunch", "tongue stretch", "swimming lesson"];	
let userInput = prompt("Which activity do you want?")

userInput <= 3 && userInput >= 0 ? print(activities[userInput]) : print(activities[0]);

